1. Closed means that server responds to SYN with [RST, ACK] that means i received your SYN but there is no process behind this process. check screenshot1.png
2. Filtered means that server doesn't respond to SYN at all(possibly beacuase iptable blocked this port). check screenshot2.png
3. REQUESTS=POST, OPTIONS, PROPFIND
